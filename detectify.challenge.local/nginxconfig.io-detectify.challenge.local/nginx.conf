# Generated by nginxconfig.io
# https://www.digitalocean.com/community/tools/nginx#?0.domain=detectify.challenge.local&0.path=%2Fusr%2Fshare%2Fnginx%2Fhtml&0.document_root=%2F&0.redirect=false&0.https=false&0.php=false&0.index=index.html&0.fallback_php=false&0.access_log_domain&0.error_log_domain&referrer_policy=origin&server_tokens&limit_req&log_not_found&user=root

user root;
pid /run/nginx.pid;
worker_processes auto;
worker_rlimit_nofile 65535;

events {
	multi_accept on;
	worker_connections 65535;
}

http {
	charset utf-8;
	sendfile on;
	tcp_nopush on;
	tcp_nodelay on;
	types_hash_max_size 2048;
	client_max_body_size 16M;

	# MIME
	include mime.types;
	default_type application/octet-stream;

	# logging
	access_log /var/log/nginx/access.log;
	error_log /var/log/nginx/error.log warn;

	# limits
	limit_req_log_level warn;
	limit_req_zone $binary_remote_addr zone=login:10m rate=10r/m;

	# load configs
	include /etc/nginx/conf.d/*.conf;
	include /etc/nginx/sites-enabled/*;
}
